/**
 * The release version as source code — the one literal `scripts/release.mjs`
 * bumps outside the manifests. The MCP handshake reports it and the pane
 * shows it; `tests/version.test.ts` pins it to `package.json`.
 */
export const VERSION = '0.27.1';
